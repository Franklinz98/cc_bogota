enum AppBarType { start, content }
enum EventCardType { current, future }
enum AuthScreen { login, forgotten }
enum AuthType { leader, admin, none }
enum CredentialOp { signUp, grantAdmin, grantLeader, removeCredentials }
enum ContentScreen { home, media, ministries, pfi, donations, school, contact, view, pp, tc }
enum ContentViews { our_vision,  our_history,  location,  womens,  entrepenours,  kids,  r21 }
enum PfiScreen {a, b, c}
enum ViewType {carroussel, image, text, redirect}
